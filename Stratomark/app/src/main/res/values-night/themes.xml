<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Stratomark" parent="Theme.Default">
        <!-- statusbar -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- divider (manual) -->
        <item name="android:strokeColor">@color/grey5</item>

        <!-- bg -->
        <item name="android:colorBackground">@color/grey2</item>

        <!-- surface -->
        <item name="colorSurface">@color/grey1</item>
        <item name="colorOnSurface">@color/white</item>
    </style>

</resources>